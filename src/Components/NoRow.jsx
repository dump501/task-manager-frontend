import { Stack, Typography } from "@mui/material";

const NoRow = () => (
  <Stack height="100%" alignItems="center" justifyContent="center">
    <Typography variant="h3">No Data to display now</Typography>
  </Stack>
);

export default NoRow;
